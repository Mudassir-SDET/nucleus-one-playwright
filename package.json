{
  "name": "nucleus-one-playwright",
  "version": "1.0.0",
  "scripts": {
    "clean:js": "rimraf allure-results/js",
    "test": "npm run clean:js && playwright test",
    "report": "allure generate allure-results/js --clean -o allure-report && allure open allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "allure-commandline": "^2.38.1",
    "allure-playwright": "^3.6.0",
    "rimraf": "^6.1.3"
  }
}